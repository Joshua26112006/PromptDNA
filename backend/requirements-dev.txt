# Development / test dependencies — PromptDNA backend

-r requirements.txt

pytest>=8,<9
# httpx is a runtime dependency (see requirements.txt) — the test client
# (fastapi.testclient / starlette) reuses it, so it is not repeated here.
